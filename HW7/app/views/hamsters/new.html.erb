<!DOCTYPE html>

<h3>
	<%= "New hampster for #{@user.email}" %>
</h3>

<div>
	<%= form_for([@user, @hamster], method: :post) do |f| %>
		<%= f.label :name %>
		<%= f.text_field :name %>
		<%= f.label :phrase %>
		<%= f.text_area :phrase %>
		<%= f.submit "Make New Hammy" %>
	<% end %>
</div>